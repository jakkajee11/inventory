{
  "name": "@inventory/utils",
  "version": "1.0.0",
  "description": "Shared utility functions for Inventory Management System",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "inventory",
    "utils",
    "utilities"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@inventory/types": "workspace:*"
  },
  "devDependencies": {
    "@inventory/eslint-config": "workspace:*",
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "restricted"
  }
}
